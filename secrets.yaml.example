# Copy to secrets.yaml and fill in your WiFi credentials.
# Only WiFi lives here: no web password, no API encryption, no OTA password
# (trusted home LAN). Copy to secrets.yaml and edit.

wifi_ssid: "your_ssid"
wifi_password: "your_password"
